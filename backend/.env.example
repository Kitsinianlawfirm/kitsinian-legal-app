# Server Configuration
PORT=3000
NODE_ENV=development

# Database (Render PostgreSQL)
DATABASE_URL=postgres://user:password@host:5432/database

# Admin API Key (generate a secure random string)
ADMIN_API_KEY=your-secure-admin-api-key-here

# Encryption Key for PII (AES-256)
# Generate with: node scripts/generate-encryption-key.js
# CRITICAL: Keep this secret, never commit to git
ENCRYPTION_KEY=generate-a-64-character-hex-key-using-the-script

# Email Configuration (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@kitsinianlaw.com

# Lead Notifications
LEAD_NOTIFICATION_EMAIL=intake@kitsinianlaw.com

# CORS (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Logging
LOG_LEVEL=info
